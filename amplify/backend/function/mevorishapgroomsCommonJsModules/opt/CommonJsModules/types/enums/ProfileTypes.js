/**
 * @typedef {string} ProfileTypes
 * @enum {ProfileTypes}
 */
const ProfileTypes = {
  ROOM_PROVIDER: "ROOM_PROVIDER",
  ROOM_TENANT: "ROOM_TENANT",
};

/**
 * @typedef {"ROOM_PROVIDER" | "ROOM_TENANT"} Types
 */

module.exports = ProfileTypes;
