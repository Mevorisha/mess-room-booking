/**
 * Get human readable name / address of location by latitude and longitude
 * @param {number} lat
 * @param {number} lon
 * @returns {Promise<string>}
 */
async function getLocationName(lat, lon) {
    return "Location Name";
}

module.exports = getLocationName;
